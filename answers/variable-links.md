**1**

```typescript
type UserInputProps = {
  value: string
}

const UserInput = ({ value }: UserInputProps) => {

  // в данном примере я использую связь 3-го вида.
  const parsedValue = parseInt(value)

  // Обосновывается это тем, что UserInput является компонентом представления
  // он постоянно ре-рендерится и в value, которое в него приходит постоянно изменяется
  // и новое значение нужно отображать в инпуте.

  // соответственно, при каждом рендере мы должны правильно распрасить приходящее value в число.

  // P.S. на практике используются функции мемоизаторы, для того, что бы парсить value
  // только в том случае, если оно изменилось, но что бы не нагружать пример, я их опустил.

}
```

**2**
```typescript
const ADMIN_ROLE = 'ADMIN'

class AdminProfile extends UserProfile {

  constructor() {
    this.name = ADMIN_ROLE
  }
}

// в данном случае позднее присваивание через константу.
// У каждой роли есть свой код, в данном случае 'ADMIN', 
// и к этому коду мы можем довольно часто обращаться и внутри программы. 
// Поэтому в данном случае нам нужна широкая гибкость, на случай, если мы заходим поменять код 'ADMIN' на другой.
```

**3**
```typescript

const notificationStyles = {
  color: 'red',
  width: 250,
  height: 100,
  // ...
}
// в данном случае я делаю мгновенное присваивание т.к. это является jsx-стилями для блока нотификаций.
// Они принадлежат исключительно ему.
// Если я хочу изменить внешний вид, то я просто зайду в этот компонент, увижу эти стили и изменю их, 
// при этом, я не хочу побочного действия в виде изменения внешнего вида где-то еще.
```