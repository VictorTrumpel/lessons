**1**
```javascript
const signActions = [] 

// Массив куда записываются все дейсвия подписания документа.
// Он нужен только для того, что бы потом отменять последнее подписание.
// В данном случае эта структура данных не безопасна, т.к. программист имеет доступ ко всем подписаниям,
// не только к предыдущему.
// Также, программист может бесконтрольно добавлять элементы в массив.

// В таком случае, самой оптимальной структурой будет Стек.

// в массивах js-та уже реализованы методы стека: push(), pop()

// решения:
// 1. - либо написать коммент, что бы прогрммист-коллека пользовался только методами push(), pop()
// 2. - типизировать данный массив при помощи TS-а таким образом, 
// что бы у него был доступ только к методам push() и pop()
// 3. - создать новую абстракцию, SignerActionsHistory - и общаться с ней только по предоставленным 
// контрактам.
```

**2**
```javascript
const treeData = []
// нашел такую переменную состояния в легаси коде своего проекта. Очевидно писал какой-то джун.
// во-первых название переменной не говорящее. На самом деле она должна называться так:

const companiesTree = []

// ну и конечно же, тут будет подходить гораздо лучше такая структура как Дерево,
// где каждый узел - это компания и она имеет массив ссылок на компании подрядчиков.
```

**3**
```javascript
const fightHistory = [] 
// массив из игры. В него мы записываем все события, которые случаются в файтинге для того,
// что бы реализовать перемещение во времени назад.

// Это можно реализовать в виде стека, так же как и в примере 1.

```

**4**
```javascript
// При обходе графа в ширину мы записываем в массив "соседей" в массив, для того, что бы обойти их.
// Это можно представить в виде очереди: первым зашел, первым вышел.

const queue = []

while (queue.length) {
  // пока есть ноды - обрабатываем их
}

// в этом случае, мы тоже можем типизировать через TS так, что бы были доступны только
// push(), shift()

// либо написать слой для взаимодействия с этим массивом
```

**5**
```javascript

const currencies = [[1, 2, 3, 4], [1, 2, 3, 4]] // матрица валют. каждый индекс соответствует какой-либо валюте
// в массиве под соответствующим индексом представлены номиналы этой валюты по-возрастанию.

// Матрица в данном случае неудобна т.к. нужно где-то хранить константы, которые указывают на то, 
// какому индексу какая валюта соответствует.

// лучше представить в виде объекта:

const currencies = {
  'USD': [1, 2, 3, 4],
  'RUB': [1, 2, 3, 4],
  // ...
}
```






